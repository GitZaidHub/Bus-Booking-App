# Bus-Booking-App
 introduce relationships between tables using foreign keys in Sequelize. By creating associations, we ensure that data is linked properly, allowing efficient queries and management of related records.
